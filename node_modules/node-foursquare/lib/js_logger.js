JSLogger = function() {
  this.getLogger= function(config)  {
     return {
       warn : function(text) {
         console.log(text);
       },
       error : function(text) {
         console.log(text);
       },
       debug : function(text) {
         console.log(text);
       }      
     };
  };
};

module.exports = {
   'logger' : new JSLogger().getLogger()
};
